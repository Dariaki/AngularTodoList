import {Pipe, PipeTransform} from '@angular/core';import ITodo from './todo.interface';@Pipe({  name: 'todoTitleFilter'})export class TodoFilterPipe implements PipeTransform {  transform(todolist: ITodo[], searchTerm: string): ITodo[] {    if (!todolist || !searchTerm) return todolist;    return todolist.filter(todo => todo.title.toLowerCase().indexOf(searchTerm.toLowerCase()) !== -1);  }}